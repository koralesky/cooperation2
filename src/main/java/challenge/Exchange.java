package challenge;

import java.math.BigDecimal;

public class Exchange {

    BigDecimal currencyToExchange;
    BigDecimal defaultExchangeRate;

     Boolean giveCurrencyToExchange(BigDecimal amount) {

         //pobiera kwotę i zapisuje ją w polu currencyToExchange

        return true;
    }

    BigDecimal getCurrencyAfterConversion(BigDecimal currentRate){
         BigDecimal currencyAfterConversion;

         //konwertuje walutę na inną przy użyciu parametru reprezentującą kurs

         return currencyAfterConversion;
    }

}
